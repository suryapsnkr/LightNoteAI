# Required for prompt parsing (OpenAI / Gemini)
OPENAI_API_KEY=sk-...

# Redis (Celery broker/backend)
REDIS_URL=redis://localhost:6379/0

# Optional: use Gemini for vision instead of local models
GEMINI_API_KEY=...